# llmgrid-context

Context assembly and memory for llmgrid. Part of [llmgrid](https://github.com/sanketn26/llmgrid).

**Status:** alpha. APIs will change between `0.x` releases. Not yet implemented; this release reserves the package in the llmgrid family.

```bash
pip install llmgrid-context
```

```python
import llmgrid.context
```
